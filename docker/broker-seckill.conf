brokerClusterName = SeckillCluster
brokerName = seckill-broker-a
brokerId = 0
brokerRole = ASYNC_MASTER
flushDiskType = ASYNC_FLUSH
namesrvAddr = namesrv:9876
autoCreateTopicEnable = true
# The service runs on the Windows host in local mode, so advertise the host loopback address.
brokerIP1 = 127.0.0.1
listenPort = 11911
